package com.navitech.navi.data.services

class GPSService {
    /*var gpsTracker: GPSTracker = GPSTracker(this)

    var listCall: Call<WeatherPOJO> = service.getWeather(
        gpsTracker.getLatitude(),
        gpsTracker.getLongitude(),
        "metric",
        passwordString
    )*/
}